# Pip requirements file for development


# Base packages
# Must be consistent with build-system.requires in pyproject.toml

pip>=23.3
setuptools>=70.0.0
wheel>=0.41.3


# Direct and necessary indirect dependencies for development

build>=1.0.0
bump2version>=1.0.0
